/*
 * lw_effect_resurrect.c -- 1:1 port of effect/EffectResurrect.java
 *
 * Java class body is empty (no apply override). No-op in C as well.
 */
#include <stddef.h>

#include "lw_effect.h"


void lw_effect_resurrect_apply(LwEffect *self, struct LwState *state) {
    (void) self;
    (void) state;
    /* Java body is empty -- no-op. */
}
